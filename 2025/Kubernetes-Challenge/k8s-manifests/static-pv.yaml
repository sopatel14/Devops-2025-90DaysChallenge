apiVersion: v1
kind: PersistentVolume
metadata:
  name: static-nginx-pv
  labels:
    type: local
    app: nginx
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: "/data/nginx-static"
